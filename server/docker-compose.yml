version: "3.9"

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  smtp-drain:
    build: .

    ports:
      - "3000:3000"
      - "1025:1025"

    environment:
      REDIS_URL: redis://redis:6379

    depends_on:
      - redis